app_identifier("com.egi-engineer.Paceria")

# 公開リポジトリのため Team ID は環境変数から読む。
# ローカル実行時は .env（gitignore 済み）から供給される。fastlane が自動で
# 読むのは .env / .env.default / .env.<環境名> のみ。.env.fastlane という名前は
# --env fastlane を付けない限り読まれないので使わない。
team_id(ENV.fetch("DEVELOPMENT_TEAM"))
